
User reports two bugs on AI Twin page (`/ai-twin`):
1. **Can't delete reference images** — hover-to-delete on reference image grid doesn't work.
2. **"Click first image to lock as reference" doesn't match** — clicking the first reference image to generate angles produces a person that doesn't match that reference.

Let me investigate the relevant code.
