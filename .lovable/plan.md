
User wants the "Recreate with VEO3" flow on `/video-repo-pro` to use a better prompt — specifically one that makes sense for VEO3 (which supports native audio/dialogue) and lets the character actually talk.

Need to look at how `recreateWithVeo3` builds the prompt today.
